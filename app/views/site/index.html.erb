<div class="content">
  <%= render 'site/form' %>
  <h1 class="title">TechSearch</h1>
</div>
<div>
  <table class="table" style="table-layout:fixed">
    <thead>
      <tr>
        <th style="width:1200px">タイトル</th>
        <th style="width:400px">サイト名</th>
      </tr>
    </thead>
    <% @sites.each do |site| %>
    <tbody>
      <tr>
        <td><%= link_to "#{site.title}", site.url %></td>
        <td><%= site.name %></td>
        <td> 
         
        </td>
      </tr>
    </tbody>
    <% end %>
  </table>
</div>